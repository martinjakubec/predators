import {PreyName} from '../Prey/prey';
import {MapData} from './types';

const map01: MapData = {
  size: 7,
  data: [
    {
      animal: PreyName.Rabbit,
      coordinates: [0, 1],
    },
  ],
};

export default map01;
