import {Grid} from './Grid';

function App() {

  
  return (
    <div className="container mx-auto p-10 flex justify-center">
      <Grid gridHeight={15} gridWidth={15} />
    </div>
  );
}

export default App;
